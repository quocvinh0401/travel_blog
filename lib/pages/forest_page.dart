import 'package:flutter/material.dart';

class ForestPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Forest Page'),),
    );
  }
}
